# Dispatch 0569: Add fail-fast mutation verification

## Controller metadata

- **Sequential dispatch:** `0569` of `597`
- **Dependency wave:** `213`
- **Conflict-free parallel batch:** `01` within this wave
- **Original group:** `Group 181: Create the full verification script`
- **Original micro-step IDs:** `181.12`
- **Phase:** Phase 12 — Packaging, documentation, and live acceptance
- **Prerequisite dispatch files:** `steps/0568__w212p01__g181__181.11__execute-full-verification-script.md`
- **Would-be parallel peers:** None
- **Verification capabilities required:** `full_verification_passed`, `node_environment`, `node_lockfile`, `pi_smoke_verified`, `python_lockfile`
- **Capabilities produced:** `failfast_mutation_suite_passed`

## Goal

Prove that the combined verification script fails at the correct stage for each supported deliberate defect.

This dispatch performs only the micro-step below. It must not implement later behavior.

## Exact task envelope

### Target files

- `scripts/test-verify-all-failfast.sh`
- `runtime/tests/contract/test_verify_all_pipeline.py`

### Allowed edits

Only the paths listed under **Target files**. If **Target files** is None, do not edit checked-in source.

### Forbidden edits

- Unrelated modules, tests, documentation, or configuration.
- Frozen public contracts, numeric defaults, or provider boundaries.
- Credential files, real tokens, passwords, OAuth codes, or provider client-secret contents.
- Behavior belonging to a later dispatch.

### Primary symbols and signature contracts

- **Primary symbol:** None
  - **Reason:** Test-harness dispatch; no production callable is introduced.

### Inputs, fixtures, and where to look

- `reference/pi-playlist-bridge-plan-v2.6.md` → `### Group 181: Create the full verification script`
- `scripts/verify-all.sh`
- `scripts/verify-pi-extension.sh`
- `tools/validate_dispatch_package.py`
- **Expected libraries:** POSIX shell, uv, Ruff, mypy, pytest/pytest-cov, npm, TypeScript, Pi CLI

## Frozen constraints

- Run from a clean checkout with fake/local configuration; no live provider accounts are permitted.
- Restore or isolate every deliberate mutation.
- Never weaken a verification threshold to make a test pass.
- Do not edit production behavior.

## Original micro-step `181.12` — Add fail-fast mutation verification

- **Original dependency:** Step 181.11
- **Implement:** Create the shell harness and pytest contract test. Each mutation runs in an isolated temporary repository copy and is removed afterward.
- **Acceptance:** All eight mutations fail at the expected stage, later stages do not run, the source checkout is unchanged, and the clean pipeline still passes.
- **Stop rule:** The acceptance check passes. Do not begin the next micro-step.

## Verification

- **Kind:** Automated, exact commands
- **Working directory:** `repository root`

Run these commands in order:

```bash
bash scripts/test-verify-all-failfast.sh && cd runtime && uv run pytest tests/contract/test_verify_all_pipeline.py -q && cd .. && bash scripts/verify-all.sh
```

Every command must exit zero. Run the acceptance check for every original micro-step separately, even when one command covers the bundle.

## Required completion report

```text
Dispatch: 0569
Step IDs: 181.12
Changed files:
- <exact path, or "None — verification only">
Verification:
- Working directory: repository root
- Command/checklist: bash scripts/test-verify-all-failfast.sh && cd runtime && uv run pytest tests/contract/test_verify_all_pipeline.py -q && cd .. && bash scripts/verify-all.sh

Results:
- 181.12: PASS|FAIL — <relevant assertion/output>
Deferred work:
- No later dispatch was implemented.
```
