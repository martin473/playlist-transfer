# Dispatch 0503: Implement `runCliProcess`

## Controller metadata

- **Sequential dispatch:** `0503` of `597`
- **Dependency wave:** `187`
- **Conflict-free parallel batch:** `01` within this wave
- **Original group:** `Group 160: Spawn without a shell`
- **Original micro-step IDs:** `160.06`
- **Phase:** 11 — Pi extension
- **Prerequisite dispatch files:** `steps/0052__w022p01__g190__190.03__test-boundary-type-and-schema-alignment.md`, `steps/0419__w153p01__g161__161.05__flush-final-complete-fragment.md`, `steps/0480__w182p01__g160__160.05__capture-stderr-separately.md`, `steps/0501__w186p01__g162__162.04-162.05__force-terminate-remaining-child-verify-no-orphan-process.md`, `steps/0502__w186p02__g163__163.04__return-valid-diagnostic-report-path.md`
- **Would-be parallel peers:** None
- **Verification capabilities required:** `node_environment`, `node_test_script`, `ts_boundary_types`
- **Capabilities produced:** `cli_process_runner`

## Goal

Expose the frozen public process-aggregation function required by every Pi workflow tool.

This dispatch performs only the micro-step below. It must not implement later behavior.

## Exact task envelope

### Target files

- `extension/process.ts`
- `extension/test/process.test.ts`

### Allowed edits

Only the paths listed under **Target files**.

### Forbidden edits

- Unrelated modules, tests, documentation, or configuration.
- Any lockfile.
- Frozen public contracts except the exact new contract declared below.
- Credential files, real tokens, passwords, OAuth codes, or provider client-secret contents.
- Behavior belonging to a later dispatch.

### Primary symbols and signature contracts

- **Primary symbol:** `runCliProcess`
  - **Contract kind:** `callable`
  - **Signature contract:** `(invocation: CliInvocation, signal: AbortSignal, onEvent: (event: PlaylistBridgeEvent) => void) => Promise<ProcessResult>`
  - **Typed errors:** `['ProcessSpawnError', 'ProcessProtocolError']`
  - **Allowed side effects:** `['child_process_spawn', 'filesystem_report_write']`
  - **Registered owner file:** `extension/process.ts`
  - **Contract source:** reference/symbol-contracts.yaml

### Inputs, fixtures, and where to look

- `reference/pi-playlist-bridge-plan-v2.6.md` → `### Group 160`
- `reference/symbol-contracts.yaml` for the authoritative cross-step contract
- `reference/dispatch-symbol-contracts.yaml` for this dispatch envelope
- Existing private helpers in `extension/process.ts`
- Existing tests in `extension/test/process.test.ts`

## Frozen constraints

- Preserve literal argument arrays and `shell: false`.
- Keep child output bounded and diagnostics in report files.
- Do not introduce shell command strings.
- Do not implement later dispatches.

## Original micro-step `160.06` — Implement `runCliProcess`

- **Original dependency:** Step 160.05, Step 161.05, Step 162.05, Step 163.04, and Step 190.03
- **Implement:** Call `spawnCli`; connect `JsonlEventParser`; capture bounded stdout/stderr; attach `AbortSignal` cancellation; await child completion; and return `ProcessResult` without duplicating the private helper logic.
- **Acceptance:** A fixture child stream produces parsed events and bounded diagnostics, cancellation terminates the child, and the returned `ProcessResult` matches the frozen contract.
- **Stop rule:** The acceptance check passes. Do not begin the next micro-step.

## Verification

- **Kind:** Automated, exact commands
- **Working directory:** `extension`

Run these commands in order:

```bash
npm test -- test/process.test.ts
```

Every command must exit zero. Run the acceptance check for every original micro-step separately, even when one command covers the bundle.

## Required completion report

```text
Dispatch: 0503
Step IDs: 160.06
Changed files:
- <exact path>
Verification:
- Working directory: extension
- Command/checklist: npm test -- test/process.test.ts
Results:
- 160.06: PASS|FAIL — <relevant assertion/output>
Deferred work:
- No later dispatch was implemented.
```
