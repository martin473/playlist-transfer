# Dispatch 0533: Document review tool usage

## Controller metadata

- **Sequential dispatch:** `0533` of `597`
- **Dependency wave:** `197`
- **Conflict-free parallel batch:** `01` within this wave
- **Original group:** `Group 175: Create the skill file`
- **Original micro-step IDs:** `175.03`
- **Phase:** 12 — Packaging, documentation, and live acceptance
- **Prerequisite dispatch files:** `steps/0529__w196p01__g175__175.02__document-transfer-tool-usage.md`
- **Would-be parallel peers:** `steps/0534__w197p01__g186__186.04__revoke-google-youtube-provider-grant.md`, `steps/0535__w197p01__g177__177.04__document-output-redaction.md`, `steps/0536__w197p01__g173__173.03__add-jsonl-chunk-parser-unit-test.md`
- **Verification capabilities required:** None
- **Capabilities produced:** None

## Goal

Write `SKILL.md` explaining that Pi must call the registered tools, must not handle tokens, and must preserve explicit transfer modes.

This dispatch performs only the bundled micro-steps listed below. It must not implement later behavior.

## Exact task envelope

### Target files

- `SKILL.md`

### Allowed edits

Only the paths listed under **Target files**. If **Target files** is None, do not edit checked-in source; produce only the stated generated artifact or acceptance evidence. A directory target permits only files required by these micro-steps inside that directory.

### Forbidden edits

- Unrelated modules, tests, documentation, or configuration.
- Any lockfile unless that exact lockfile appears under **Target files**.
- Frozen public contracts, numeric defaults, or provider boundaries.
- Credential files, real tokens, passwords, OAuth codes, or provider client-secret contents.
- Behavior belonging to a later dispatch.

### Primary symbols and signature contracts

- **Primary symbol:** None
  - **Reason:** No callable is introduced. This dispatch changes configuration, tests, fixtures, documentation, generated artifacts, scripts, packaging, or acceptance evidence only.

### Inputs, fixtures, and where to look

- `reference/pi-playlist-bridge-plan-v2.6.md` → `### Group 175: Create the skill file`
- `reference/pi-playlist-bridge-plan-v2.6.md` → `## Group target-file registry`
- `reference/pi-playlist-bridge-plan-v2.6.md` → `## Frozen cross-boundary model contracts`
- `reference/symbol-contracts.yaml` for public cross-step signatures
- `reference/dispatch-symbol-contracts.yaml` for this dispatch's complete symbol envelope
- **Expected libraries:** Markdown; Pi skills mechanism
- **Original source dependency:** Steps 164–166, 168, 170–171
- **Verification ownership:** documentation, scope, attribution, and configuration checks

## Frozen constraints

- Use the exact target paths and primary symbols above; do not create aliases or competing modules.
- Public boundaries reject unknown fields and do not expose provider payload shapes.
- Never store or print provider credentials.
- Machine-readable stdout remains valid JSON/JSONL; diagnostics go to stderr or report files.
- The Pi extension uses literal argument arrays with `shell: false`.
- Do not implement later dispatches.

## Original micro-step `175.03` — Document review tool usage

- **Original dependency:** Step 175.02
- **Implement:** State when Pi must call `playlist_review`.
- **Acceptance:** List and apply actions are named.
- **Stop rule:** The acceptance check passes. Do not begin the next micro-step.

## Verification

- **Kind:** Automated, exact commands
- **Working directory:** `repository root`

Run these commands in order:

```bash
python3 - <<'PY'
from pathlib import Path
p=Path('SKILL.md'); assert p.is_file()
text=p.read_text().lower()
assert 'playlist_review' in text, 'playlist_review'
assert 'list' in text, 'list'
assert 'apply' in text, 'apply'
PY
```

Every command must exit zero. Run the acceptance check for every original micro-step separately, even when one command covers the bundle.

## Required completion report

```text
Dispatch: 0533
Step IDs: 175.03
Changed files:
- <exact path, or "None — verification/evidence only">
Verification:
- Working directory: repository root
- Command/checklist: python3 - <<'PY' from pathlib import Path p=Path('SKILL.md'); assert p.is_file() text=p.read_text().lower() assert 'playlist_review' in text, 'playlist_review' assert 'list' in text, 'list' assert 'apply' in text, 'apply' PY

Results:
- 175.03: PASS|FAIL — <relevant assertion/output or evidence path>
Deferred work:
- No later dispatch was implemented.
```
