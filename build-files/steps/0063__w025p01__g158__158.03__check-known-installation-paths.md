# Dispatch 0063: Check known installation paths

## Controller metadata

- **Sequential dispatch:** `0063` of `597`
- **Dependency wave:** `025`
- **Conflict-free parallel batch:** `01` within this wave
- **Original group:** `Group 158: Locate the CLI executable`
- **Original micro-step IDs:** `158.03`
- **Phase:** 11 — Pi extension
- **Prerequisite dispatch files:** `steps/0035__w016p01__g156__156.04__add-extension-typecheck-script.md`, `steps/0040__w017p01__g156__156.05__add-extension-test-script.md`, `steps/0044__w018p01__g156__156.06-156.07__generate-extension-package-lock-json-verify-clean-npm-ci.md`, `steps/0052__w022p01__g190__190.03__test-boundary-type-and-schema-alignment.md`, `steps/0058__w024p01__g158__158.01-158.02__read-explicit-cli-executable-configuration-validate-configured-executabl.md`
- **Would-be parallel peers:** `steps/0064__w025p01__g040__040.03-040.04__implement-load-token-test-load-token.md`, `steps/0065__w025p01__g022__022.01__define-transferrequest.md`, `steps/0066__w025p01__g056__056.07__define-temporaryproviderfailure.md`, `steps/0067__w025p01__g010__010.03__add-mypy-command.md`
- **Verification capabilities required:** `node_environment`, `node_test_script`, `node_typecheck_script`, `ts_boundary_types`
- **Capabilities produced:** None

## Goal

Implement a deterministic lookup using explicit configuration first and known installation paths second; do not search arbitrary PATH entries silently.

This dispatch performs only the bundled micro-steps listed below. It must not implement later behavior.

## Exact task envelope

### Target files

- `extension/process.ts`

### Allowed edits

Only the paths listed under **Target files**. If **Target files** is None, do not edit checked-in source; produce only the stated generated artifact or acceptance evidence. A directory target permits only files required by these micro-steps inside that directory.

### Forbidden edits

- Unrelated modules, tests, documentation, or configuration.
- Any lockfile unless that exact lockfile appears under **Target files**.
- Frozen public contracts, numeric defaults, or provider boundaries.
- Credential files, real tokens, passwords, OAuth codes, or provider client-secret contents.
- Behavior belonging to a later dispatch.

### Primary symbols and signature contracts

- **Primary symbol:** `locatePlaylistBridgeExecutable`
  - **Contract kind:** `callable`
  - **Signature contract:** `(configuredPath?: string) => Promise<string>`
  - **Typed errors:** `['ExecutableNotFoundError']`
  - **Allowed side effects:** `['filesystem_read']`
  - **Registered owner file:** `extension/process.ts`
  - **Contract source:** reference/symbol-contracts.yaml

### Inputs, fixtures, and where to look

- `reference/pi-playlist-bridge-plan-v2.6.md` → `### Group 158: Locate the CLI executable`
- `reference/pi-playlist-bridge-plan-v2.6.md` → `## Group target-file registry`
- `reference/pi-playlist-bridge-plan-v2.6.md` → `## Frozen cross-boundary model contracts`
- `reference/symbol-contracts.yaml` for public cross-step signatures
- `reference/dispatch-symbol-contracts.yaml` for this dispatch's complete symbol envelope
- **Expected libraries:** `node:fs`, `node:path`, `node:process`
- **Original source dependency:** Steps 006, 156–157
- **Verification ownership:** `extension/test/process.test.ts`

## Frozen constraints

- Use the exact target paths and primary symbols above; do not create aliases or competing modules.
- Public boundaries reject unknown fields and do not expose provider payload shapes.
- Never store or print provider credentials.
- Machine-readable stdout remains valid JSON/JSONL; diagnostics go to stderr or report files.
- The Pi extension uses literal argument arrays with `shell: false`.
- Do not implement later dispatches.

## Original micro-step `158.03` — Check known installation paths

- **Original dependency:** Step 158.02
- **Implement:** When no explicit path is configured, check only the documented known installation paths in deterministic order.
- **Acceptance:** The first valid known path is returned.
- **Stop rule:** The acceptance check passes. Do not begin the next micro-step.

## Verification

- **Kind:** Automated, exact commands
- **Working directory:** `extension`

Run these commands in order:

```bash
npm run typecheck
```

Every command must exit zero. Run the acceptance check for every original micro-step separately, even when one command covers the bundle.

## Required completion report

```text
Dispatch: 0063
Step IDs: 158.03
Changed files:
- <exact path, or "None — verification/evidence only">
Verification:
- Working directory: extension
- Command/checklist: npm run typecheck

Results:
- 158.03: PASS|FAIL — <relevant assertion/output or evidence path>
Deferred work:
- No later dispatch was implemented.
```
