# Dispatch 0341: Persist match decision

## Controller metadata

- **Sequential dispatch:** `0341` of `597`
- **Dependency wave:** `109`
- **Conflict-free parallel batch:** `01` within this wave
- **Original group:** `Group 111: Create the single-track matcher`
- **Original micro-step IDs:** `111.08`
- **Phase:** 8 — Deterministic candidate matching
- **Prerequisite dispatch files:** `steps/0017__w012p01__g004__004.02-004.03-004.04__synchronize-development-tools-generate-uv-lock-and-verify-frozen-synchro.md`, `steps/0199__w055p01__g189__189.03-189.04__define-repository-and-supporting-ports.md`, `steps/0340__w108p01__g111__111.07__create-match-decision.md`
- **Would-be parallel peers:** None
- **Verification capabilities required:** `python_lockfile`, `repository_ports`
- **Capabilities produced:** None

## Goal

For one source track: normalize, classify, check correction, check cache, search, rank, decide, persist, and return one decision.

This dispatch performs only the bundled micro-steps listed below. It must not implement later behavior.

## Exact task envelope

### Target files

- `runtime/src/playlist_bridge/matching/matcher.py`

### Allowed edits

Only the paths listed under **Target files**. If **Target files** is None, do not edit checked-in source; produce only the stated generated artifact or acceptance evidence. A directory target permits only files required by these micro-steps inside that directory.

### Forbidden edits

- Unrelated modules, tests, documentation, or configuration.
- Any lockfile unless that exact lockfile appears under **Target files**.
- Frozen public contracts, numeric defaults, or provider boundaries.
- Credential files, real tokens, passwords, OAuth codes, or provider client-secret contents.
- Behavior belonging to a later dispatch.

### Primary symbols and signature contracts

- **Primary symbol:** `match_source_track`
  - **Contract kind:** `callable`
  - **Signature contract:** `(track: SourceTrack, policy: MatchPolicy, market: str | None, dependencies: MatcherDependencies, cancel: CancellationToken) -> MatchDecision`
  - **Typed errors:** `['AuthenticationRequired', 'PermissionDenied', 'RateLimited', 'InvalidProviderResponse', 'TemporaryProviderFailure', 'CancellationRequested']`
  - **Allowed side effects:** `['sqlite_read', 'sqlite_write', 'provider_network']`
  - **Registered owner file:** `runtime/src/playlist_bridge/matching/matcher.py`
  - **Contract source:** reference/symbol-contracts.yaml

### Inputs, fixtures, and where to look

- `reference/pi-playlist-bridge-plan-v2.6.md` → `### Group 111: Create the single-track matcher`
- `reference/pi-playlist-bridge-plan-v2.6.md` → `## Group target-file registry`
- `reference/pi-playlist-bridge-plan-v2.6.md` → `## Frozen cross-boundary model contracts`
- `reference/symbol-contracts.yaml` for public cross-step signatures
- `reference/dispatch-symbol-contracts.yaml` for this dispatch's complete symbol envelope
- **Expected libraries:** Spotify adapter, normalization, repositories
- **Original source dependency:** Steps 036, 079–081, 088, 108–110
- **Verification ownership:** `runtime/tests/unit/matching/test_matcher.py`

## Frozen constraints

- Use the exact target paths and primary symbols above; do not create aliases or competing modules.
- Public boundaries reject unknown fields and do not expose provider payload shapes.
- Never store or print provider credentials.
- Machine-readable stdout remains valid JSON/JSONL; diagnostics go to stderr or report files.
- The Pi extension uses literal argument arrays with `shell: false`.
- Do not implement later dispatches.

## Original micro-step `111.08` — Persist match decision

- **Original dependency:** Step 111.07
- **Implement:** Upsert the one-track decision and cache only eligible accepted automatic matches.
- **Acceptance:** The persisted decision reloads with the same status.
- **Stop rule:** The acceptance check passes. Do not begin the next micro-step.

## Verification

- **Kind:** Automated, exact commands
- **Working directory:** `runtime`

Run these commands in order:

```bash
uv run python - <<'PY'
import importlib
m=importlib.import_module('playlist_bridge.matching.matcher')
assert hasattr(m,'match_source_track'), 'match_source_track'
PY
```

Every command must exit zero. Run the acceptance check for every original micro-step separately, even when one command covers the bundle.

## Required completion report

```text
Dispatch: 0341
Step IDs: 111.08
Changed files:
- <exact path, or "None — verification/evidence only">
Verification:
- Working directory: runtime
- Command/checklist: uv run python - <<'PY' import importlib m=importlib.import_module('playlist_bridge.matching.matcher') assert hasattr(m,'match_source_track'), 'match_source_track' PY

Results:
- 111.08: PASS|FAIL — <relevant assertion/output or evidence path>
Deferred work:
- No later dispatch was implemented.
```
