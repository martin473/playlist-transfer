# Dispatch 0425: Implement `build_runtime_dependencies_for_job`

## Controller metadata

- **Sequential dispatch:** `0425` of `597`
- **Dependency wave:** `155`
- **Conflict-free parallel batch:** `01` within this wave
- **Original group:** `Group 189: Define ports and the production composition root`
- **Original micro-step IDs:** `189.19`
- **Phase:** 3A — Ports and composition root
- **Prerequisite dispatch files:** `steps/0424__w154p01__g189__189.18__implement-build-runtime-dependencies.md`
- **Would-be parallel peers:** None
- **Verification capabilities required:** `python_lockfile`, `runtime_composition_root`
- **Capabilities produced:** `runtime_job_composition_root`

## Goal

Reconstruct production runtime dependencies for resume and review continuation using the persisted job profile aliases.

This dispatch performs only the bundled micro-steps listed below. It must not implement later behavior.

## Exact task envelope

### Target files

- `runtime/src/playlist_bridge/bootstrap.py`
- `runtime/tests/unit/test_bootstrap.py`

### Allowed edits

Only the paths listed under **Target files**. Test-only dispatches must not modify production modules. Do not edit any unlisted file.

### Forbidden edits

- Unrelated modules, tests, documentation, or configuration.
- Any lockfile unless that exact lockfile appears under **Target files**.
- Frozen public contracts except the exact contracts declared below.
- Credential files, real tokens, passwords, OAuth codes, or provider client-secret contents.
- Behavior belonging to a later dispatch.

### Primary symbols and signature contracts

- **Primary symbol:** `build_runtime_dependencies_for_job`
  - **Contract kind:** `callable`
  - **Signature contract:** `(job_id: str, *, state: ApplicationState | None = None, market: str | None = None) -> RuntimeDependencies`
  - **Typed errors:** `['JobNotFoundError', 'AuthenticationRequired', 'CredentialCorruptionError', 'PermissionDenied', 'InvalidProviderResponse', 'TemporaryProviderFailure', 'OSError', 'SQLAlchemyError', 'MigrationError']`
  - **Allowed side effects:** `['filesystem_directory_create', 'sqlite_open', 'sqlite_migration', 'sqlite_read', 'os_keychain_read', 'os_keychain_write', 'provider_network', 'secure_random']`
  - **Registered owner file:** `runtime/src/playlist_bridge/bootstrap.py`
  - **Contract source:** reference/symbol-contracts.yaml


### Inputs, fixtures, and where to look

- `reference/pi-playlist-bridge-plan-v2.6.md` → `### Group 189: Define ports and the production composition root`
- `reference/symbol-contracts.yaml` for authoritative cross-step signatures
- `reference/dispatch-symbol-contracts.yaml` for this dispatch's complete envelope
- Existing functions/classes named in the original dependency fields below; do not duplicate them.

## Frozen constraints

- Use exact target paths and symbol names; do not create aliases or competing modules.
- Protocols hide SQLAlchemy sessions and concrete keyring backends from orchestration and CLI code.
- Production composition builders are the only CLI path to database, keychain, and provider construction.
- Never store or print provider credentials.
- Do not implement later dispatches.

## Original micro-step `189.19` — Implement `build_runtime_dependencies_for_job`

- **Original dependency:** Step 189.18 and Group 035
- **Implement:** Load the job from `ApplicationState.jobs`, read its stored source and Spotify profile aliases, and delegate to `build_runtime_dependencies`.
- **Acceptance:** Unknown jobs fail before provider access; resumed jobs use the same named profiles and concrete repositories as their original run.
- **Stop rule:** The acceptance check passes. Do not begin the next micro-step.


## Verification

- **Kind:** Automated, exact commands
- **Working directory:** `runtime`

Run these commands in order:

```bash
uv run pytest tests/unit/test_bootstrap.py -q
```

Every command must exit zero. Run the acceptance check for every original micro-step separately.

## Required completion report

```text
Dispatch: 0425
Step IDs: 189.19
Changed files:
- <exact path>
Verification:
- Working directory: runtime
- Commands: uv run pytest tests/unit/test_bootstrap.py -q
Results:
- 189.19: PASS|FAIL — <relevant assertion/output>
Deferred work:
- No later dispatch was implemented.
```
