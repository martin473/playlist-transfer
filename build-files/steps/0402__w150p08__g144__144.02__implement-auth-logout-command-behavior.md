# Dispatch 0402: Implement `auth logout` command behavior

## Controller metadata

- **Sequential dispatch:** `0402` of `597`
- **Dependency wave:** `150`
- **Conflict-free parallel batch:** `08` within this wave
- **Original group:** `Group 144: Create the `auth logout` command`
- **Original micro-step IDs:** `144.02`
- **Phase:** 10 — User-facing CLI
- **Prerequisite dispatch files:** `steps/0017__w012p01__g004__004.02-004.03-004.04__synchronize-development-tools-generate-uv-lock-and-verify-frozen-synchro.md`, `steps/0204__w060p01__g189__189.14-189.15__define-and-build-authentication-dependencies.md`, `steps/0391__w149p09__g144__144.01__declare-auth-logout-command-signature.md`
- **Would-be parallel peers:** None
- **Verification capabilities required:** `auth_composition_root`, `python_lockfile`
- **Capabilities produced:** None

## Goal

Delete one profile token after explicit confirmation unless `--yes` is supplied.

This dispatch performs only the bundled micro-steps listed below. It must not implement later behavior.

## Exact task envelope

### Target files

- `runtime/src/playlist_bridge/cli.py`

### Allowed edits

Only the paths listed under **Target files**. If **Target files** is None, do not edit checked-in source; produce only the stated generated artifact or acceptance evidence. A directory target permits only files required by these micro-steps inside that directory.

### Forbidden edits

- Unrelated modules, tests, documentation, or configuration.
- Any lockfile unless that exact lockfile appears under **Target files**.
- Frozen public contracts, numeric defaults, or provider boundaries.
- Credential files, real tokens, passwords, OAuth codes, or provider client-secret contents.
- Behavior belonging to a later dispatch.

### Primary symbols and signature contracts

- **Primary symbol:** `auth_logout`
  - **Contract kind:** `callable`
  - **Signature contract:** `(service: str, profile_name: str, output: OutputMode = OutputMode.human) -> None`
  - **Typed errors:** `['ConfigurationError']`
  - **Allowed side effects:** `['os_keychain_delete', 'sqlite_read', 'stdout_write', 'stderr_write']`
  - **Registered owner file:** `runtime/src/playlist_bridge/cli.py`
  - **Contract source:** reference/symbol-contracts.yaml

### Inputs, fixtures, and where to look

- `reference/pi-playlist-bridge-plan-v2.6.md` → `### Group 144: Create the `auth logout` command`
- `reference/pi-playlist-bridge-plan-v2.6.md` → `## Group target-file registry`
- `reference/pi-playlist-bridge-plan-v2.6.md` → `## Frozen cross-boundary model contracts`
- `reference/symbol-contracts.yaml` for public cross-step signatures
- `reference/dispatch-symbol-contracts.yaml` for this dispatch's complete symbol envelope
- **Expected libraries:** `typer`
- **Original source dependency:** Steps 034, 054, 153
- **Verification ownership:** `runtime/tests/unit/test_cli_commands.py`

## Frozen constraints

- Use the exact target paths and primary symbols above; do not create aliases or competing modules.
- Public boundaries reject unknown fields and do not expose provider payload shapes.
- Never store or print provider credentials.
- Machine-readable stdout remains valid JSON/JSONL; diagnostics go to stderr or report files.
- The Pi extension uses literal argument arrays with `shell: false`.
- Do not implement later dispatches.

## Original micro-step `144.02` — Implement `auth logout` command behavior

- **Original dependency:** Step 144.01
- **Implement:** Accept service/profile and `--yes`; otherwise request explicit confirmation before logout.
- **Acceptance:** A fake dependency receives the expected arguments.
- **Stop rule:** The acceptance check passes. Do not begin the next micro-step.

## Verification

- **Kind:** Automated, exact commands
- **Working directory:** `runtime`

Run these commands in order:

```bash
uv run pytest tests/unit/test_cli_commands.py -q
```

Every command must exit zero. Run the acceptance check for every original micro-step separately, even when one command covers the bundle.

## Required completion report

```text
Dispatch: 0402
Step IDs: 144.02
Changed files:
- <exact path, or "None — verification/evidence only">
Verification:
- Working directory: runtime
- Command/checklist: uv run pytest tests/unit/test_cli_commands.py -q

Results:
- 144.02: PASS|FAIL — <relevant assertion/output or evidence path>
Deferred work:
- No later dispatch was implemented.
```
