# Dispatch 0568: Execute the full verification script

## Controller metadata

- **Sequential dispatch:** `0568` of `597`
- **Dependency wave:** `212`
- **Conflict-free parallel batch:** `01` within this wave
- **Original group:** `Group 181: Create the full verification script`
- **Original micro-step IDs:** `181.11`
- **Phase:** Phase 12 — Packaging, documentation, and live acceptance
- **Prerequisite dispatch files:** `steps/0567__w211p01__g181__181.10__validate-build-plan-dependency-graph.md`
- **Would-be parallel peers:** None
- **Verification capabilities required:** `node_environment`, `node_lockfile`, `pi_smoke_verified`, `python_lockfile`
- **Capabilities produced:** `full_verification_passed`

## Goal

Execute the completed fail-fast verification pipeline from a clean repository state.

This dispatch performs only the micro-step below. It must not implement later behavior.

## Exact task envelope

### Target files

- None

### Allowed edits

Only the paths listed under **Target files**. If **Target files** is None, do not edit checked-in source.

### Forbidden edits

- Unrelated modules, tests, documentation, or configuration.
- Frozen public contracts, numeric defaults, or provider boundaries.
- Credential files, real tokens, passwords, OAuth codes, or provider client-secret contents.
- Behavior belonging to a later dispatch.

### Primary symbols and signature contracts

- **Primary symbol:** None
  - **Reason:** Verification-only dispatch; no production callable is introduced.

### Inputs, fixtures, and where to look

- `reference/pi-playlist-bridge-plan-v2.6.md` → `### Group 181: Create the full verification script`
- `scripts/verify-all.sh`
- `scripts/verify-pi-extension.sh`
- `tools/validate_dispatch_package.py`
- **Expected libraries:** POSIX shell, uv, Ruff, mypy, pytest/pytest-cov, npm, TypeScript, Pi CLI

## Frozen constraints

- Run from a clean checkout with fake/local configuration; no live provider accounts are permitted.
- Restore or isolate every deliberate mutation.
- Never weaken a verification threshold to make a test pass.
- Do not edit production behavior.

## Original micro-step `181.11` — Execute the full verification script

- **Original dependency:** Step 181.10
- **Implement:** Run `bash scripts/verify-all.sh` from the repository root and retain its stage output.
- **Acceptance:** The command exits zero and every declared verification stage executes.
- **Stop rule:** The acceptance check passes. Do not begin the next micro-step.

## Verification

- **Kind:** Automated, exact commands
- **Working directory:** `repository root`

Run these commands in order:

```bash
bash scripts/verify-all.sh
```

Every command must exit zero. Run the acceptance check for every original micro-step separately, even when one command covers the bundle.

## Required completion report

```text
Dispatch: 0568
Step IDs: 181.11
Changed files:
- <exact path, or "None — verification only">
Verification:
- Working directory: repository root
- Command/checklist: bash scripts/verify-all.sh

Results:
- 181.11: PASS|FAIL — <relevant assertion/output>
Deferred work:
- No later dispatch was implemented.
```
