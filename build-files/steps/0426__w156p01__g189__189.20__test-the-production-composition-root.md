# Dispatch 0426: Test the production composition root

## Controller metadata

- **Sequential dispatch:** `0426` of `597`
- **Dependency wave:** `156`
- **Conflict-free parallel batch:** `01` within this wave
- **Original group:** `Group 189: Define ports and the production composition root`
- **Original micro-step IDs:** `189.20`
- **Phase:** 3A — Ports and composition root
- **Prerequisite dispatch files:** `steps/0425__w155p01__g189__189.19__implement-build-runtime-dependencies-for-job.md`
- **Would-be parallel peers:** None
- **Verification capabilities required:** `python_lockfile`, `runtime_job_composition_root`
- **Capabilities produced:** `composition_root_verified`

## Goal

Prove that the real CLI-facing builders compose directories, migrations, repository adapters, keychain ports, provider factories, and runtime dependency bundles without passing raw sessions or credentials through the CLI.

This dispatch performs only the bundled micro-steps listed below. It must not implement later behavior.

## Exact task envelope

### Target files

- `runtime/tests/integration/test_bootstrap_composition.py`

### Allowed edits

Only the paths listed under **Target files**. Test-only dispatches must not modify production modules. Do not edit any unlisted file.

### Forbidden edits

- Unrelated modules, tests, documentation, or configuration.
- Any lockfile unless that exact lockfile appears under **Target files**.
- Frozen public contracts except the exact contracts declared below.
- Credential files, real tokens, passwords, OAuth codes, or provider client-secret contents.
- Behavior belonging to a later dispatch.

### Primary symbols and signature contracts

- **Primary symbol:** None
  - **Reason:** Test-only dispatch; production modules are forbidden targets.

### Inputs, fixtures, and where to look

- `reference/pi-playlist-bridge-plan-v2.6.md` → `### Group 189: Define ports and the production composition root`
- `reference/symbol-contracts.yaml` for authoritative cross-step signatures
- `reference/dispatch-symbol-contracts.yaml` for this dispatch's complete envelope
- Existing functions/classes named in the original dependency fields below; do not duplicate them.

## Frozen constraints

- Use exact target paths and symbol names; do not create aliases or competing modules.
- Protocols hide SQLAlchemy sessions and concrete keyring backends from orchestration and CLI code.
- Production composition builders are the only CLI path to database, keychain, and provider construction.
- Never store or print provider credentials.
- Do not implement later dispatches.

## Original micro-step `189.20` — Test the production composition root

- **Original dependency:** Step 189.19
- **Implement:** Using a temporary SQLite database, fake keyring backend, and fake provider client factories, call the auth, jobs, review, source, runtime, and resume builders and then invoke a dry-run transfer through the same dependency path used by the CLI.
- **Acceptance:** The test reaches the runner, lists and shows the recorded job through `build_job_query_dependencies`, loads review repositories including source tracks, returns reports, performs no destination write in dry run, and exposes no Session or token payload at the CLI boundary.
- **Stop rule:** The acceptance check passes. Do not begin the next micro-step.


## Verification

- **Kind:** Automated, exact commands
- **Working directory:** `runtime`

Run these commands in order:

```bash
uv run pytest tests/integration/test_bootstrap_composition.py -q
```

Every command must exit zero. Run the acceptance check for every original micro-step separately.

## Required completion report

```text
Dispatch: 0426
Step IDs: 189.20
Changed files:
- <exact path>
Verification:
- Working directory: runtime
- Commands: uv run pytest tests/integration/test_bootstrap_composition.py -q
Results:
- 189.20: PASS|FAIL — <relevant assertion/output>
Deferred work:
- No later dispatch was implemented.
```
