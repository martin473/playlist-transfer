# Dispatch 0437: Run source-loading stage

## Controller metadata

- **Sequential dispatch:** `0437` of `597`
- **Dependency wave:** `159`
- **Conflict-free parallel batch:** `01` within this wave
- **Original group:** `Group 138: Create the top-level run function`
- **Original micro-step IDs:** `138.04`
- **Phase:** 9 — Resumable transfer job runner
- **Prerequisite dispatch files:** `steps/0017__w012p01__g004__004.02-004.03-004.04__synchronize-development-tools-generate-uv-lock-and-verify-frozen-synchro.md`, `steps/0434__w158p01__g138__138.03__acquire-the-job-writer-lease.md`
- **Would-be parallel peers:** `steps/0435__w159p01__g166__166.03__define-explicit-transfer-mode.md`, `steps/0436__w159p01__g150__150.04__render-safe-job-error-details.md`
- **Verification capabilities required:** `python_lockfile`
- **Capabilities produced:** None

## Goal

Define the typed runtime dependency bundle, acquire a single-writer lease, and compose all existing stages without duplicating provider, matching, or batching logic.

This dispatch performs only the bundled micro-steps listed below. It must not implement later behavior.

## Exact task envelope

### Target files

- `runtime/src/playlist_bridge/jobs/runner.py`

### Allowed edits

Only the paths listed under **Target files**. If **Target files** is None, do not edit checked-in source; produce only the stated generated artifact or acceptance evidence. A directory target permits only files required by these micro-steps inside that directory.

### Forbidden edits

- Unrelated modules, tests, documentation, or configuration.
- Any lockfile unless that exact lockfile appears under **Target files**.
- Frozen public contracts, numeric defaults, or provider boundaries.
- Credential files, real tokens, passwords, OAuth codes, or provider client-secret contents.
- Behavior belonging to a later dispatch.

### Primary symbols and signature contracts

- **Primary symbol:** `run_transfer`
  - **Contract kind:** `callable`
  - **Signature contract:** `(request: TransferRequest, *, dependencies: RuntimeDependencies, emit: EventEmitter, cancel: CancellationToken) -> TransferResult`
  - **Typed errors:** `['JobBusyError', 'LeaseLostError', 'AuthenticationRequired', 'PermissionDenied', 'ProviderNotFound', 'RateLimited', 'InvalidProviderResponse', 'TemporaryProviderFailure', 'CancellationRequested', 'OSError']`
  - **Allowed side effects:** `['sqlite_read', 'sqlite_write', 'provider_network', 'destination_write', 'filesystem_write']`
  - **Registered owner file:** `runtime/src/playlist_bridge/jobs/runner.py`
  - **Contract source:** reference/symbol-contracts.yaml

### Inputs, fixtures, and where to look

- `reference/pi-playlist-bridge-plan-v2.6.md` → `### Group 138: Create the top-level run function`
- `reference/pi-playlist-bridge-plan-v2.6.md` → `## Group target-file registry`
- `reference/pi-playlist-bridge-plan-v2.6.md` → `## Frozen cross-boundary model contracts`
- `reference/symbol-contracts.yaml` for public cross-step signatures
- `reference/dispatch-symbol-contracts.yaml` for this dispatch's complete symbol envelope
- **Expected libraries:** All runtime components
- **Original source dependency:** Steps 022, 117, 120–137
- **Verification ownership:** `runtime/tests/integration/test_runner.py`

## Frozen constraints

- Use the exact target paths and primary symbols above; do not create aliases or competing modules.
- Public boundaries reject unknown fields and do not expose provider payload shapes.
- Never store or print provider credentials.
- Machine-readable stdout remains valid JSON/JSONL; diagnostics go to stderr or report files.
- The Pi extension uses literal argument arrays with `shell: false`.
- Do not implement later dispatches.

## Original micro-step `138.04` — Run source-loading stage

- **Original dependency:** Step 138.03
- **Implement:** Invoke the source-loading stage only when its persisted checkpoint is incomplete and heartbeat the lease at the documented interval.
- **Acceptance:** A resumed completed source stage is not repeated and heartbeat uses the current lease token.
- **Stop rule:** The acceptance check passes. Do not begin the next micro-step.

## Verification

- **Kind:** Automated, exact commands
- **Working directory:** `runtime`

Run these commands in order:

```bash
uv run python - <<'PY'
import importlib
m=importlib.import_module('playlist_bridge.jobs.runner')
assert hasattr(m,'RuntimeDependencies'), 'RuntimeDependencies'
assert hasattr(m,'run_transfer'), 'run_transfer'
PY
```

Every command must exit zero. Run the acceptance check for every original micro-step separately, even when one command covers the bundle.

## Required completion report

```text
Dispatch: 0437
Step IDs: 138.04
Changed files:
- <exact path, or "None — verification/evidence only">
Verification:
- Working directory: runtime
- Command/checklist: uv run python - <<'PY' import importlib m=importlib.import_module('playlist_bridge.jobs.runner') assert hasattr(m,'RuntimeDependencies'), 'RuntimeDependencies' assert hasattr(m,'run_transfer'), 'run_transfer' PY

Results:
- 138.04: PASS|FAIL — <relevant assertion/output or evidence path>
Deferred work:
- No later dispatch was implemented.
```
