# Dispatch 0052: Test boundary-type and schema alignment

## Controller metadata

- **Sequential dispatch:** `0052` of `597`
- **Dependency wave:** `022`
- **Conflict-free parallel batch:** `01` within this wave
- **Original group:** `Group 190: Define TypeScript boundary types`
- **Original micro-step IDs:** `190.03`
- **Phase:** 3B — TypeScript boundary contracts
- **Prerequisite dispatch files:** `steps/0051__w021p01__g190__190.01-190.02__define-pi-extension-boundary-types.md`
- **Would-be parallel peers:** None
- **Verification capabilities required:** `node_environment`, `node_test_script`, `ts_boundary_types_defined`
- **Capabilities produced:** `ts_boundary_types`

## Goal

Add compile-time fixtures and runtime tests proving the schemas align with the authoritative TypeScript boundary types without permitting production edits.

This dispatch performs only the bundled micro-steps listed below. It must not implement later behavior.

## Exact task envelope

### Target files

- `extension/test/types.test.ts`

### Allowed edits

Only the paths listed under **Target files**. Test-only dispatches must not modify production modules. Do not edit any unlisted file.

### Forbidden edits

- Unrelated modules, tests, documentation, or configuration.
- Any lockfile unless that exact lockfile appears under **Target files**.
- Frozen public contracts except the exact contracts declared below.
- Credential files, real tokens, passwords, OAuth codes, or provider client-secret contents.
- Behavior belonging to a later dispatch.

### Primary symbols and signature contracts

- **Primary symbol:** None
  - **Reason:** Test-only dispatch; production TypeScript modules are forbidden targets.

### Inputs, fixtures, and where to look

- `reference/pi-playlist-bridge-plan-v2.6.md` → `### Group 190: Define TypeScript boundary types`
- `reference/symbol-contracts.yaml` for authoritative cross-step signatures
- `reference/dispatch-symbol-contracts.yaml` for this dispatch's complete envelope
- Existing functions/classes named in the original dependency fields below; do not duplicate them.

## Frozen constraints

- Use exact target paths and symbol names; do not create aliases or competing modules.
- Protocols hide SQLAlchemy sessions and concrete keyring backends from orchestration and CLI code.
- Production composition builders are the only CLI path to database, keychain, and provider construction.
- Never store or print provider credentials.
- Do not implement later dispatches.

## Original micro-step `190.03` — Test boundary-type and schema alignment

- **Original dependency:** Step 190.02
- **Implement:** Add compile-time fixtures for every valid and invalid authoritative tool-input variant. These fixtures are consumed by the later schema dispatches; do not import schemas that have not been created yet.
- **Acceptance:** Type checking passes, valid fixtures satisfy the exact input types, and invalid action/visibility fixtures are rejected by TypeScript. Runtime schema parity is verified when Steps 164.04 and 166.05 create the schemas.
- **Stop rule:** The acceptance check passes. Do not begin the next micro-step.


## Verification

- **Kind:** Automated, exact commands
- **Working directory:** `extension`

Run this command:

```bash
npm run typecheck
```

The command must exit zero.

## Required completion report

```text
Dispatch: 0052
Step IDs: 190.03
Changed files:
- <exact path>
Verification:
- Working directory: extension
- Commands: npm run typecheck
Results:
- 190.03: PASS|FAIL — <relevant assertion/output>
Deferred work:
- No later dispatch was implemented.
```
