# Dispatch 0024: Test declare runtime dependencies

## Controller metadata

- **Sequential dispatch:** `0024` of `597`
- **Dependency wave:** `013`
- **Conflict-free parallel batch:** `01` within this wave
- **Original group:** `Group 003: Declare runtime dependencies`
- **Original micro-step IDs:** `003.02`
- **Phase:** 1 — Repository and executable skeleton
- **Prerequisite dispatch files:** `steps/0013__w009p01__g003__003.01__implement-declare-runtime-dependencies.md`, `steps/0017__w012p01__g004__004.02-004.03-004.04__synchronize-development-tools-generate-uv-lock-and-verify-frozen-synchro.md`
- **Would-be parallel peers:** `steps/0018__w013p01__g156__156.01__create-extension-package-metadata.md`, `steps/0019__w013p01__g013__013.01__define-sourceservice.md`, `steps/0020__w013p01__g093__093.01-093.02__implement-chunk-spotify-uris-test-chunk-spotify-uris.md`, `steps/0021__w013p01__g066__066.01-066.02__implement-iso-8601-video-durations-test-iso-8601-video-durations.md`, `steps/0022__w013p01__g006__006.02__test-empty-typer-application.md`, `steps/0023__w013p01__g002__002.04__verify-build-discovery.md`
- **Verification capabilities required:** `python_lockfile`
- **Capabilities produced:** `runtime_environment_verified`

## Goal

Add the listed libraries to the runtime dependency section without writing application code.

This dispatch performs only the bundled micro-steps listed below. It must not implement later behavior.

## Exact task envelope

### Target files

- None

### Allowed edits

Only the paths listed under **Target files**. If **Target files** is None, do not edit checked-in source; produce only the stated generated artifact or acceptance evidence. A directory target permits only files required by these micro-steps inside that directory.

### Forbidden edits

- Unrelated modules, tests, documentation, or configuration.
- Any lockfile unless that exact lockfile appears under **Target files**.
- Frozen public contracts, numeric defaults, or provider boundaries.
- Credential files, real tokens, passwords, OAuth codes, or provider client-secret contents.
- Behavior belonging to a later dispatch.

### Primary symbols and signature contracts

- **Primary symbol:** None
  - **Reason:** No callable is introduced. This dispatch changes configuration, tests, fixtures, documentation, generated artifacts, scripts, packaging, or acceptance evidence only.

### Inputs, fixtures, and where to look

- `reference/pi-playlist-bridge-plan-v2.6.md` → `### Group 003: Declare runtime dependencies`
- `reference/pi-playlist-bridge-plan-v2.6.md` → `## Group target-file registry`
- `reference/pi-playlist-bridge-plan-v2.6.md` → `## Frozen cross-boundary model contracts`
- `reference/symbol-contracts.yaml` for public cross-step signatures
- `reference/dispatch-symbol-contracts.yaml` for this dispatch's complete symbol envelope
- **Expected libraries:** `typer`, `pydantic`, `platformdirs`, `spotipy`, `google-api-python-client`, `google-auth`, `google-auth-oauthlib`, `keyring`, `SQLAlchemy`, `alembic`, `rapidfuzz`, `tenacity`, `isodate`
- **Original source dependency:** Step 002
- **Verification ownership:** clean/frozen environment checks

## Frozen constraints

- Use the exact target paths and primary symbols above; do not create aliases or competing modules.
- Public boundaries reject unknown fields and do not expose provider payload shapes.
- Never store or print provider credentials.
- Machine-readable stdout remains valid JSON/JSONL; diagnostics go to stderr or report files.
- The Pi extension uses literal argument arrays with `shell: false`.
- Do not implement later dispatches.

## Original micro-step `003.02` — Test declare runtime dependencies

- **Original dependency:** Step 003.01
- **Implement:** Add or run one focused automated test or deterministic verification check for this requirement: The project environment installs without dependency-resolution errors.
- **Acceptance:** The project environment installs without dependency-resolution errors.
- **Stop rule:** The acceptance check passes. Do not begin the next micro-step.

## Verification

- **Kind:** Automated, exact commands
- **Working directory:** `runtime`

Run these commands in order:

```bash
uv sync --frozen
```

```bash
uv run python - <<'PY'
import typer,pydantic,platformdirs,spotipy,keyring,sqlalchemy,alembic,rapidfuzz,tenacity,isodate
import googleapiclient,google.auth,google_auth_oauthlib
print('runtime dependencies import successfully')
PY
```

Every command must exit zero. Run the acceptance check for every original micro-step separately, even when one command covers the bundle.

## Required completion report

```text
Dispatch: 0024
Step IDs: 003.02
Changed files:
- <exact path, or "None — verification/evidence only">
Verification:
- Working directory: runtime
- Command/checklist: uv sync --frozen; uv run python - <<'PY' import typer,pydantic,platformdirs,spotipy,keyring,sqlalchemy,alembic,rapidfuzz,tenacity,isodate import googleapiclient,google.auth,google_auth_oauthlib print('runtime dependencies import successfully') PY

Results:
- 003.02: PASS|FAIL — <relevant assertion/output or evidence path>
Deferred work:
- No later dispatch was implemented.
```
